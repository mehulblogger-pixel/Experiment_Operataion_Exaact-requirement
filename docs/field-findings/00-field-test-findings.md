# Field-Test Findings Register (client walkthrough)

Raised from a live walkthrough. **Nothing is being changed yet** — this is the agreed checklist. We work
through it **one at a time, in an order you approve** (no simultaneous work). Point numbers match the
original list.

**Type:** 🐞 Bug · 🧭 UX/Flow · 🎯 Feature/Gap · ❓ Analysis-first (the "is this a bug? be brutal" ones).
**Sev (first-pass, to confirm):** P0 data-integrity/scope · P1 workflow-breaking · P2 important · P3 nice-to-have.
**Status:** ☐ open · ◐ in progress · ✅ done.

---

## A. Client registration / masters

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 1 | 🎯 | P2 | **No document upload** under Client Registration → Registration tab. | ☐ |
| 6 | 🐞 | P2 | **"Find on maps" / "Pick on app"** (site location picker) **not working**. | ☐ |

## B. Lead → Opportunity → Quotation (the sales spine)

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 2 | 🎯 | P2 | When a client has **no quote**, allow **adding a quote from the Purchase Order tab** under registration. | ☐ |
| 4 | ❓🐞 | P1 | **Opportunity dashboard** shows "still to generate quotation" **even though a quote was raised** (activity shows quotation submitted/won) — yet still offers "attach/generate new quotation". *Bug or intentional? → brutal analysis when we reach it.* **✅ FIXED — verdict: bug.** *Brutal analysis:* the system had **two disagreeing definitions of "which quotes are on this deal."** `quote_linked_deal_id()` (used by raise-order and sync-on-accept) already treats a quote raised **off the deal's lead** (`quotations.lead_id = opportunities.lead_id`) as belonging to the deal. But `opp_quotes()` — which drives the Quotes tab, the "Generate quotation" gate, the delete-guard and the order-of-record — listed **only** rows in the `opportunity_quotes` link table, and that link is written **only when the quote is ACCEPTED** (crm.php sync-on-accept). So a merely **submitted** lead-quote was invisible on the deal: it showed in the activity log but the deal still said "still to quote" and offered to generate another. *Fix (read-side, non-destructive — no schema/permission/lifecycle change):* broadened `opp_quotes()` to also include quotes tied through the shared lead (deduped with the explicit links), so every screen now uses **one** definition; excluded those same lead-quotes from the "attach an existing quotation" dropdown so a listed quote isn't also offered for attach. Guarded so a **leadless** deal never slurps the client's unrelated quotes (only explicit links count there). Also hardened a SQLite integer/string type-affinity foot-gun in the exclusion (`CAST(? AS INTEGER)`). Test `test_field04_lead_quote_on_deal.php` (7 assertions). Suite **3836/0**. | ✅ |
| 5 | 🐞 | P1 | A **lead moved to Opportunity/Deal** must transfer and **appear there**, and the board must update. Currently doesn't. **✅ FIXED.** *Investigation:* the transfer engine (`opp_from_lead` → `opp_create`, `Work this as a deal →`) is actually **sound** — it opens the deal on the **default OPPORTUNITY pipeline's first stage**, carries the customer/value/contact across, links `lead_id`, and the deal **does** show on the opportunity board (reproduced + now pinned end-to-end). Two forward paths exist and both work: the deal button (→ opportunity board), and moving a lead to its pipeline's **Won** stage (→ conversion to customer+inquiry, `lead_move` returns `convert`). *Root cause of "currently doesn't":* the **LEADS board — where the salesperson is standing — showed no sign the lead had become a deal**, so the transfer looked like it never happened. *Fix (non-destructive, additive; no new permission/status):* `lead_board()` now marks (one batch query, off the register's hot path) every card that has a deal opened from it, and the board card shows a **"◆ deal"** chip; the lead detail already carried **"Open the deal →"**. Also verified opening a deal twice returns the same deal (no duplicate), and that a scoped board still shows office-less deals (`scope_office_clause` includes `NULL`). Test `test_field05_lead_to_deal_transfer.php` (12 assertions, end-to-end). Suite **3863/0**. | ✅ |
| 7 | 🎯 | P2 | **Lead generation:** choose **manday vs manmonth** deputation / technical manpower — how many, what skills/qualifications. **Manday → no site details; manmonth → site details required.** (Conditional intake.) | ☐ |
| 8 | ❓🐞 | P1 | **Lead stays "qualified", not "converted"** after the quote is submitted **and won** (quote is attached to the lead); and the **Opportunity doesn't show this lead as converted**. *Bug? → brutal analysis.* **✅ FIXED — verdict: bug.** *Brutal analysis:* "Qualified" is a **stage**; the missing state is the **status** `CONVERTED` (`LEAD_STATUS` = OPEN/CONVERTED/LOST). A quotation can be raised **straight off a lead** (`quotations.lead_id` set, files carried) **without** first converting it. On acceptance, the quote-sync already sent the outcome to the **deal** (moved to WON) and the **client** (landed on the master) — but **never back to the lead**. So a lead that produced a won quote directly sat at OPEN/"Qualified" forever, and every screen judging conversion by `status='CONVERTED'`/`converted_*` showed it as still in play; the deal kept nagging "Convert the lead" because its `partner_id` was never filled. *Fix (non-destructive; existing status + existing OPEN→CONVERTED transition — no new status/permission):* added `lead_convert_on_quote_win()` and called it from the quote **ACCEPTED** branch — marks the lead CONVERTED with the just-landed customer + `converted_at`, moves it to the lead pipeline's WON stage, and fills the originating deal's customer (so the "Convert the lead" nudges disappear and `See the lead` shows it converted). Deliberately does **not** create a duplicate customer or raise a duplicate inquiry (both already exist by acceptance time). Idempotent (a CONVERTED/LOST lead is left alone) and best-effort (never blocks the acceptance). Documented the acceptance side-effect in `docs/03-object-lifecycles.md` (Quotation §). Test `test_field08_lead_converts_on_win.php` (15 assertions). Suite **3851/0**. | ✅ |

## C. Contract

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 3 | 🎯 | P1 | **Add a contract:** "Qty sold" must be **multi-line** (multiple items — mandays / man-month / other); if **no quotation/line item exists, allow adding it inline**. Also add **Edit / Delete a contract** once created. | ☐ |
| 9 | 🧭 | P1 | After OM + BM **endorse the contract → jump to site address → no way back**; flow confusing. **✅ FIXED (framework + 2 flows).** *Approach (your pick):* **in-page popup (iframe modal)**. Built a reusable, **zero-per-screen** framework: a `[data-embed="/url"]` trigger opens the existing target screen in a modal iframe rendered **bare** (`embed=1`, no chrome); forms are stamped with `_embed=1` so a post-save **`redirect()` posts `{embedDone}` to the host** which **closes the popup and refreshes** — so the page you were on is never lost and every screen works in the popup untouched. Wired both named flows on **client-360**: **Raise inspection call** (`/call-new`) and **Edit / add address** (`/partner-edit`), each with a plain `href` fallback for JS-off. Test `test_field09_embed_modal.php` (15 assertions). Suite **3821/0**. *(Same framework resolves #10 — see that row.)* | ✅ |

## D. Inspection call / job allocation

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 22 | 🐞 | **P0** | **INSPECTOR SCOPE LEAK.** A call allocated to **Mr. Vijay only** is also visible in **Mr. Ravi's schedule**. **✅ FIXED.** *Root cause (confirmed + reproduced):* the schedule queries filter correctly by `inspector_id`, but the **user-edit form let two logins be linked to the same team member** (`inspectors` row) — exactly the owner's "Add to Team Member" hypothesis — so both logins resolved to one person and saw their jobs. *Fix:* one team member = one login — a save-time guard (`inspector_login_conflict`) refuses linking a second login to an already-linked member, and a §7.11 integrity check (`inspector_one_login`) flags any pre-existing collision on `/data-control` + `/system-status`. Test `test_field22_inspector_login.php` (11 assertions), suite **3751/0**. *To clear existing bad data: give the second login its own team member; the integrity flag then goes green.* | ✅ |
| 12 | ❓→🎯 | P1 | **Cross-office scope.** Ahmedabad executes a Mumbai-office call. **✅ FIXED / DEFINED.** *Verified:* the executing office (Ahmedabad) **already sees** the call and job (calls are cross-office visible; jobs scope by executing office); the allocated inspector sees their job regardless of office. *Owner's rule (confirmed):* on a **cross-office job the executing office sees ONLY its inter-office credit + an Invoiced/Not-invoiced capsule (no amount)** — all other commercial (client billing, invoice value, revenue, profit) is **contracting-office only**. *Built:* `job_commercial_view($job)` → `FULL` (master / ALL / same-office / contracting office) vs `CREDIT_ONLY` (executing office on a cross-office job); the job Money tab hides the bills, profitability and invoice panels for `CREDIT_ONLY` and shows a compact **"Your inter-office credit"** capsule instead. Test `test_field12_cross_office_commercial.php` (10 assertions). Suite **3785/0**. | ✅ |
| 26 | 🐞 | P1 | **Second inspector / contractor (Ravi) not reflected** — not shown on the operational dashboard, during job allocation, etc. **✅ FIXED.** *Root cause:* the team-member INSERT used `$b['status'] ?? 'ACTIVE'`, which **does not coalesce an empty string** — a form posting `status=''` created a **blank-status inspector**, and every deputable surface filters on `status='ACTIVE'`, so he vanished from the allocate picker *and* the operational dashboard. (A SUBCON with an explicit ACTIVE status showed fine — verified.) *Fix:* the insert now coalesces empty → ACTIVE (`($b['status'] ?? '') ?: 'ACTIVE'`), and the inspector list + dashboard capacity queries treat a **blank status as active** (`COALESCE(NULLIF(status,''),'ACTIVE')='ACTIVE'`), surfacing the already-created Ravi; a genuinely-inactive person (real non-empty status) stays hidden. Test `test_field26_contractor_visible.php` (6 assertions). Suite **3791/0**. | ✅ |
| 25 | 🐞 | P2 | A call **allocated for the 28th shows in the backlog on the 27th** (future work counted as backlog), while also correctly in Schedule/Assignment registers as allocated. **✅ FIXED.** *Diagnosis:* the dashboard *metrics* are category-correct (a future-assigned call isn't in unscheduled/ready/overdue) — the bug was only the backlog **list** (`tosrm_ops_backlog`), which listed every non-closed call. *Fix:* the backlog now excludes a call that has an open, assigned job scheduled for a **future date** (it's in hand — shown in the Schedule/Assignment registers); **overdue** (past-scheduled), **today's**, and **unassigned/unscheduled** work all stay. Test `test_field25_backlog_future.php` (6 assertions). Suite **3797/0**. | ✅ |
| 10 | 🧭 | P1 | If something is missing on **New Inspection Call** and you click to add it, **you can't get back** to the inspection-call screen after adding. (Same return-navigation problem as #9.) **✅ FIXED.** *Root cause:* the **"+ Add new" client/vendor** links opened the Directory partner form in a **separate browser window** — and when the browser blocked that popup they **navigated the tab away**, discarding everything typed on the call. *Fix (reuses #9's shell, no new screens):* the add-link now opens the **same** partner form as an **in-page popup (iframe modal)**; the picker already hands the new record back via `postMessage` to **`window.opener` *or* `window.parent`**, so the launching **dropdown selects it and the popup closes *without reloading*** — the half-filled call form is preserved intact. The activity/product/office "+ Add" already stayed in-page (quick-add modal); this closes the client/vendor gap and the popup-blocked data-loss path in one change. Test `test_field10_addlink_inplace.php` (8 assertions). Suite **3829/0**. | ✅ |
| 11 | 🐞🧭 | P1 | **Validation flow:** a missing field (e.g. **inspector not selected**) lets you proceed; only at "Allocate" does it bounce to the **first** screen. **✅ FIXED.** *Diagnosis:* the allocate form is a tabbed wizard; the "who carries it out" rule is an **either/or** (an inspector **OR** a sub-contractor), so a plain `required` can't express it — the browser let it through and only the server rejected, re-rendering on the **first** tab. app.js already jumps to an *invalid* field's tab, but nothing was invalid. *Fix:* a **client-side either/or guard** on the form blocks the submit and **opens the Engineer tab + focuses the picker** (no round-trip); the who-message now lives **on that tab**; and on any server re-render an **`error_field` hint** sends the form to the flagged field's tab too. So a missing choice always lands on the missing screen only. Test `test_field11_allocate_focus.php` (9 assertions). Suite **3806/0**. | ✅ |

## E. Report generation / IDEMS

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 13 | 🧭 | P3 | QA report opens with autofilled details, but the final button reads **"Create report & generate"** — should be **"Generate report"** first (label/flow). **✅ FIXED.** Relabelled the create button (`views/ops/idems/doc_form.php`) from **"Create report & generate IRN"** → **"Generate report"** — lead with the action, not the record-keeping jargon. The IRN-is-automatic fact is preserved (the subtitle already says "The IRN is generated automatically when you save", plus a button tooltip). Editing an existing report still reads "Save report". Pure label/flow change — no behaviour, permission or lifecycle touched. Test `test_field13_generate_report_label.php` (5 assertions). Suite **3912/0**. | ✅ |
| 14 | 🐞 | P2 | **"Improve wording"** returns *"couldn't reach writing assistance"* **even though AI is activated**. (AI integration/config.) **✅ FIXED — verdict: not an AI bug at all.** *Root cause:* the **✒️ Improve wording** button is the **rule-based** phrase helper (`/writing-assistant` → `tech_expand`, no AI), and its `fetch()` **omitted the CSRF token**. Every POST is CSRF-checked globally (`index.php:857`); a tokenless POST is rejected and **302-redirected to the HTML page**, so the JS `r.json()` threw and hit `.catch` → *"Could not reach the writing assistant."* The **✨ Polish/translate** (real AI) button worked because it already sent `_csrf` — which is exactly why the failure *looked* AI-related though AI was fine. *Fix (one line):* the Improve-wording POST now carries `_csrf` like every other. **Same defect, same fix, found in the sweep:** the **availability quick-update** POST (`availability.php`) also omitted the token and silently failed (pill never updated) — fixed too. Proven at the gate: `csrf_ok('')`/wrong = reject, real token = accept. Test `test_field14_writing_assistant_csrf.php` (8 assertions). Suite **3907/0**. | ✅ |
| 15 | 🎯 | P1 | **Prepared by** = the inspecting inspector automatically; **Reviewed by / Approved by** should be **automatic per the approval rules** — the system appears to **lack a way to set/route Reviewed-by & Approved-by**. **✅ FIXED.** *Investigation:* the routing **already exists** — `idems_approval_rules` (configurable chain by report-type/office/client/BU), `idems_approver_map` (per-inspector approver + temp cover), the per-report `report_approvals` steps and `report_vetting` (technical review), all configurable at **`/idems-approval-rules`** and **`/approver-map`**; and `idems_report_signatures()` resolves **inspector + vetter + approver + issuer**. *Root cause it "appeared to lack" it:* the sign-off resolver `idems_sigblock_rows()` only read `inspector`+`approver`, and its role regex lumped **"review" in with the inspector** — so **"Reviewed by" auto-filled with the *inspector* (the same person as Prepared-by)**, which reads as "reviewer routing doesn't work." *Fix (non-destructive; one resolver, flows to the live form, the detail view and the PDF alike):* map each sign-off role to its real workflow actor — Approved/Authorised/Endorsed → the chain's **approver**; Reviewed/Vetted/Checked/Scrutinised → the **technical vetter**; Issued/Released → the **issuer**; Prepared/Inspected/Assessed/Audited/Witnessed → the **inspector**. A role whose actor hasn't acted yet stays **blank** (no false attestation — the old code wrongly stamped the inspector as reviewer), and a hand-keyed name still overrides. Test `test_field15_signoff_routing.php` (12 assertions). Suite **3875/0**. | ✅ |
| 16 | 🎯❓ | P2 | Inspector needs to **download a draft copy** to review before sending for approval. **+ Question:** how is **vetting turned on/off** in the system? (Document the switch.) **✅ DONE.** *Part 1 (draft copy):* already worked — `ops_idems_pdf` prints any not-yet-issued report **watermarked DRAFT** (`_DRAFT` filename), and the detail's primary download button is always available (no submit/approval needed). Gap was **labelling**: the primary button now plainly reads **"📄 Download draft"** before issue (and "Download PDF/report" after), with a tooltip "read it over before submitting for review". *Part 2 (the switch — answered + documented):* vetting is **one admin toggle** — **Settings → Vetting checklist** (`/vetting-checklist`), **"Require technical vetting before approval"** = the `vetting_gate_required` setting (`idems_vetting_gate_on()`), **off by default**; ON routes `DRAFT→SUBMITTED→VETTING→…→APPROVED→ISSUED` and hard-blocks issue until vetted; **Release Notes (RN/IRN) are always exempt**. Two independent sub-settings on the same screen: `vetting_checklist_on` (show a checklist) and `vetting_checklist_require` (all points required). Documented in **`docs/edge-cases/07-vetting-review-approval.md §13`** and **`docs/04-flows/inspector.md`**. Test `test_field16_draft_and_vetting_switch.php` (13 assertions). Suite **3888/0**. | ✅ |
| 17 | 🎯 | P3 | The report's **genuineness QR/scanner** should also **provide a download link** to the report when the page is opened. | ☐ |
| 18 | 🎯 | P2 | Add a block listing **applicable standards** (Standard Number, edition, etc.) as a **list** in each inspection report. **✅ DONE.** Added a dedicated **"Applicable Standards"** section — a repeatable table (a real list): **Standard No.** · **Title / Subject** · **Edition / Year** · **Clause(s) applied** (e.g. IS 2062, ASME Sec VIII Div 1, API 650). Distinct from the existing *Reference documents* table (controlled QAP/ITP/drawings with approval codes) — this is the published-codes-and-editions block ISO-17020 reports carry. Shipped in the **default inspection-report schemas** (so new report types include it — verified on MGHIR/FEXT/IR) and added to the **report-builder palette as a one-click "📐 Applicable Standards" section** (dedupe-guarded) so any existing report type gains it in one click — the same pattern the system uses for Reference documents. It is a standard `table` field, so it renders on the fill form, the detail view and the PDF with no per-field code. Non-destructive (no permission/lifecycle change; the free-text `standards` header is untouched). Test `test_field18_applicable_standards.php` (11 assertions). Suite **3899/0**. | ✅ |

## F. Job close / hold points / expenses

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 19 | 🧭 | P2 | Once a report is **submitted, approved and locked**, the screen must offer **"Close the job"**. | ☐ |
| 20 | 🐞 | P1 | After issue, the job shows **hold points**; opening one asks close/waiver with a comment (good) — but on close/waive it **jumps back to the job main screen** and **closing one of several isn't reflected**. **✅ FIXED.** *Diagnosis:* the handler already redirects to `/job?id=X#holdpoints` and `hwp_close` correctly persists the status (verified: 2 open → close one → open 1, cleared 1). The bug was the **job screen's tab JS only understood a `#job=<tab-slug>` hash, not a bare element-id hash** like `#holdpoints` — so it opened the first tab (Overview) and the hold-points panel (on "Reports & QA") stayed hidden, reading as "main screen" and "not reflected". *Fix:* the tab init now **activates the tab containing a bare-id hash target and scrolls to it** (also opens a `<details>`), which fixes hold points *and every other in-page `#anchor` link on a tabbed screen*. Test `test_field20_holdpoint_nav.php` (9 assertions). Suite **3775/0**. | ✅ |
| 21 | 🐞 | P1 | Clicking **"Close the job"** shows the **expense sheet again**, letting you **re-enter expenses** — confusing/duplicative. **✅ FIXED.** *Diagnosis:* same root cause as #24 — the close/expense form re-appearing. #24's guard already stops the form re-showing (GET) and the second close (POST), and the success path already redirects to the job (not back to the form). #21's distinct concern is **duplicate expense rows**, and the closure-expense INSERT had **no data-layer guard**. *Fix:* record the day's closure expenses **exactly once per job** — the INSERT now runs only when no closure-expense row exists, so no path (a race, a re-submit that slips the UI) can double the engineer's claim. Test `test_field21_close_expense_once.php` (6 assertions; idempotent by construction). Suite **3766/0**. | ✅ |
| 24 | 🐞 | P1 | Even when a job is **already closed and expenses booked**, the **"Close job" button is not greyed out** and lets you **close again and re-enter expenses**. **✅ FIXED.** *Diagnosis:* the close buttons on every list/detail were already hidden by `closed_flag`, and the POST already refused a double-close — but the **GET `/job-close` form had no closed guard**, so reaching it on a closed job (stale page, back button, bookmarked form, 2nd tab, offline re-send) re-showed the expense sheet. *Fix:* one `closed_flag` guard **before the POST branch** short-circuits both GET and POST — a closed job never shows the form and never re-files expenses; message points to edit-expenses / Unlock. Test `test_field24_job_close_once.php` (9 assertions). Suite **3760/0**. | ✅ |

## G. Attendance / Mark IN–OUT

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 23 | 🎯 | P2 | **Mark IN / Mark OUT must be blocked when the schedule date doesn't match** (no marking a job on the wrong date). | ☐ |

## H. Repeat-inspection carry-forward (vendor)

| # | Type | Sev | Finding | Status |
|---|---|---|---|---|
| 27 | 🎯 | P2 | For a call on a contract/quotation, **show the last inspection at the same vendor (by date)**. Allow **forwarding the previous report to the next inspector**, who can either **continue from it (number increments 1 → 2 …)** or **create a fresh report** — their choice. **Carry the QAP forward.** If a **new/revised QAP** or an added item arrives on the inspection day, allow **uploading a new QAP that autofills/overwrites the scope** (with **hold points retained**) — overwriting **only after a pop-up confirmation**. (Large, multi-part.) | ☐ |

---

## Suggested working order (to confirm — you decide)

I'd sequence by risk, then by how many other points a fix unblocks:

1. **#22 inspector scope leak (P0)** — a segregation defect; fix first and re-verify against the §51/§35 scope work.
2. **Job-close integrity — #24, #21, #20** — close idempotency + the expense re-entry + hold-point navigation/state (they cluster).
3. **Allocation & scope — #12 (define the rule), #26 second inspector, #25 backlog date, #11 validation routing.**
4. **Navigation architecture — #9 / #10** — decide modal/inline-add vs return-links (one decision fixes both, and eases many others).
5. **Sales spine analysis — #4, #8 (brutal analysis), #5 lead→opportunity transfer.**
6. **Report features — #15 reviewed/approved routing, #16 draft + vetting switch, #18 standards block, #14 AI, #13 label, #17 QR link.**
7. **Intake & masters — #7 manday/manmonth intake, #1 doc upload, #2 quote-from-PO, #3 contract multi-line + edit/delete, #6 map picker, #23 mark-in date guard.**
8. **#27 repeat-inspection carry-forward** — largest; do last, once the report + call plumbing above is settled.

**Rules we keep (from the program):** non-destructive; no new permission without asking; test-backed; docs
in lockstep; and for anything touching money figures, explicit sign-off. Each item ships as its own change
with its own test and a status flip here.

*Register created — nothing changed. Tell me which item to start with (or approve the order above) and we
take them strictly one at a time.*
